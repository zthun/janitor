language: node_js
node_js:
  - 14
  - 15
  - 16

install:
  - yarn install

jobs:
  include:
    - stage: Lint
      script:
        - yarn lint
    - stage: Test
      script:
        - yarn test
    - stage: Build
      script:
        - yarn build
